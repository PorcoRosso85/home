{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "GenericContainer",
  "description": "ジェネリクス型",
  "$comment": "言語別取り扱い:
    - Zig: comptime Tパラメータまたはfn(comptime T: type)関数として実装
    - Lean 4: 多相型システム、依存型も含む高度な型パラメータ化をサポート
    - Rust: <T>として実装、トレイト境界による制約も可能
    - TypeScript: <T>または<T extends U>として実装、型パラメータの制約も可能
    JSONスキーマはネイティブでジェネリクスをサポートしないため、これは概念的な表現",
  "type": "object",
  "properties": {
    "type": { "type": "string" },
    "data": {}
  },
  "required": ["type", "data"]
}