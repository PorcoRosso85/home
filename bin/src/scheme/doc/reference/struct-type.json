{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "StructType",
  "description": "構造体/クラス型",
  "$comment": "言語別取り扱い:
    - Zig: struct として実装、メソッドを持つことも可能
    - Lean 4: structure として実装、依存型も表現可能
    - Rust: struct として実装、メソッドはimplブロックで定義
    - TypeScript: interface または class として実装、オブジェクト指向プログラミングをサポート",
  "type": "object",
  "properties": {
    "id": { "type": "integer" },
    "name": { "type": "string" },
    "isActive": { "type": "boolean" }
  },
  "required": ["id", "name"]
}