{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "DynamicType",
  "description": "動的型",
  "$comment": "言語別取り扱い:
    - Zig: anytype として実装、コンパイル時に解決される
    - Lean 4: 動的型付けは基本的にサポートしない（静的型付け言語）
    - Rust: dyn Trait として実装、動的ディスパッチを提供
    - TypeScript: any型として実装、型チェックを事実上無効化する
    JSONスキーマでは型指定なしの値として表現"
}