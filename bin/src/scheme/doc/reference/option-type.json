{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "OptionType",
  "description": "オプション型",
  "$comment": "言語別取り扱い:
    - Zig: ?T として実装、null可能な型として扱う
    - Lean 4: Option T として実装、None または Some xとして表現
    - Rust: Option<T> として実装、None または Some(x)として表現、Noneパターンのチェックを強制
    - TypeScript: T | null | undefined として実装、nullまたはundefinedを許容する型",
  "oneOf": [
    { "type": ["string", "number", "boolean", "object", "array"] },
    { "type": "null" }
  ]
}