{
  "type": "array",
  "items": {
    "type": "object",
    "properties": {
      "type": {
        "type": "string",
        "description": "データ型（string, integer, boolean, object, array, etc.）"
      },
      "items": {
        "type": "object",
        "description": "配列の場合、要素の型定義"
      },
      "properties": {
        "type": "object",
        "additionalProperties": {
          "$ref": "#/components/schemas/TypeDefinition"
        },
        "description": "オブジェクトの場合、プロパティの型定義（キーと型定義のペア）"
      }
    },
    "required": [
      "type"
    ]
  },
  "components": {
    "schemas": {
      "TypeDefinition": {
        "type": "object",
        "properties": {
          "type": {
            "type": "string"
          },
          "items": {
            "type": "object"
          },
          "properties": {
            "type": "object"
          }
        },
        "required": [
          "type"
        ]
      }
    }
  },
  "description": "引数または返却値の型を順に表す配列。オブジェクトや配列のネスト構造に対応。"
}
