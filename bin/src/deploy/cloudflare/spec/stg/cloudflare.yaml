# Cloudflare Resource Configuration (SOT)
# Environment: stg
version: "1.0"

r2:
  buckets:
    - name: "my-app-stg-storage"
      binding: "R2_BUCKET"

workers:
  - name: "my-app-stg-worker"
    script: "src/worker.ts"
    bindings:
      r2:
        - bucket: "my-app-stg-storage"
          binding: "R2_BUCKET"

# Note: This is a template. Copy to cloudflare.yaml and encrypt with SOPS