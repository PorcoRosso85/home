# Cloudflare Resource Configuration (SOT)
# Environment: dev
version: "1.0"

r2:
  buckets:
    - name: "my-app-dev-storage"
      binding: "R2_BUCKET"

workers:
  - name: "my-app-dev-worker"
    script: "src/worker.ts"
    bindings:
      r2:
        - bucket: "my-app-dev-storage"
          binding: "R2_BUCKET"

# Note: This is a template. Copy to cloudflare.yaml and encrypt with SOPS