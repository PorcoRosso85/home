{
  "name": "shiorin-diary",
  "private": true,
  "version": "0.1.0",
  "description": "Minimal Cloudflare Worker (staging) using KV and R2.",
  "scripts": {
    "dev": "wrangler dev -e staging",
    "deploy:staging": "wrangler deploy -e staging",
    "types": "wrangler types"
  },
  "devDependencies": {
    "wrangler": "^4.20.5"
  }
}

