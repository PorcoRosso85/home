{
  description = "GitHub CI/CD workflows";
  goal = [ "Provide automated workflows" ];
  nonGoal = [ "Production configuration" ];
  meta = { owner = [ "@opencode-dev" ]; lifecycle = "stable"; };
  output = { packages = []; apps = []; modules = []; overlays = []; devShells = []; };
}