{
  description = "GitHub workflow definitions";
  goal = [ "Define CI/CD workflows" ];
  nonGoal = [ "Runtime configuration" ];
  meta = { owner = [ "@opencode-dev" ]; lifecycle = "stable"; };
  output = { packages = []; apps = []; modules = []; overlays = []; devShells = []; };
}