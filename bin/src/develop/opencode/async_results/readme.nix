{
  description = "Asynchronous operation results";
  goal = [ "Store temporary async results" ];
  nonGoal = [ "Persistent data storage" ];
  meta = { owner = [ "@opencode-dev" ]; lifecycle = "stable"; };
  output = { packages = []; apps = []; modules = []; overlays = []; devShells = []; };
}