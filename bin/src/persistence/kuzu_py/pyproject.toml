[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "kuzu_py"
version = "0.1.1"
description = "KuzuDB thin wrapper for Nix environments"
requires-python = ">=3.12"

# Flat module structure - no packages