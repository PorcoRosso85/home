// JavaScript file

const MAX_SIZE = 100;

class JsClass {
    constructor(name) {
        this.name = name;
    }
    
    getName() {
        return this.name;
    }
}

function jsFunction(x, y) {
    return x + y;
}

const arrowFunc = (x) => x * 2;