{
  "name": "vite-rsc-cloudflare-poc",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "esbuild src/hello.ts --bundle --outfile=dist/ssr/index.js --format=esm --platform=browser",
    "dev": "wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "esbuild": "^0.24.2",
    "wrangler": "^3.101.0"
  }
}