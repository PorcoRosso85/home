# 伝えられた内容からの方針分析

## ユーザーの要求から読み取れること

### 1. 目的は何ですか？
**回答: B - PythonコードをCLIで分析したい（手段は問わない）**

根拠：
- 「poc/配下のどこかにcloneしたものがあって」→ 既存のLSMCPを試している
- 「tsがcliでつかえるがしかしpythonがcliで使えない」→ **CLI使用が目的**
- 「pythonについての解決を進めたい」→ Python対応が主目的
- LSPかどうかは言及なし、CLIで使えることが重要

### 2. 現在の実装をどうしますか？
**回答: 不明（ユーザーに確認が必要）**

気になる点：
- 「試す前に不要なものの整理＋方針のずれの修正から」
- 現在の実装（Jedi/Rope）はLSPではない → 「方針のずれ」？
- でも実用的には動作している

### 3. 優先順位は？
**回答: 実用性（すぐ使える）**

根拠：
- 「最終的にはcliとしてつかえるようにしたい」→ 使えることが最優先
- 「pythonで実際に動作するかの確認まで進めて」→ 動作確認重視
- LSP準拠かどうかの言及なし

## 追加の観察

### LSPへの期待
- 「参照検索機能使ってみて」→ コードナビゲーション機能への関心
- 「lspには、文字列のreplaceなども期待したい」→ リファクタリング機能への期待
- 「lspはすでに入っていて試した、ということ？」→ **LSP使用を期待していた**

## 結論

ユーザーは：
1. **LSMCPをCLIで使いたかった**（特にPython）
2. **LSP機能（参照検索、リネーム等）を期待**
3. **現在の実装はLSPではない** → これが「方針のずれ」

推測される要望：
- 本来のLSP（pyright等）を使った実装を見たい
- または、LSMCPがなぜPythonで動かないか理解したい