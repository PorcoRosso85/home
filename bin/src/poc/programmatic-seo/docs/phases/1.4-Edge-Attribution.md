DELETE THIS FILE AFTER PHASE 1.4 IS COMPLETED

Phase 1.4 — Edge-Attribution（UTM集計・送客レポート）

目的
- 計測価値を可視化。UTM 正規化とクリック→回遊のロールアップを提供。

スコープ
- jobs/{utm_rollup.ts,export_rollup.ts}
- infra/analytics/export.ts（D1→CSV/JSON ダンプ）
- packages/measurement/fields.md（標準フィールド定義）

実装チェックリスト
- [ ] utm_rollup.ts が events を集計し、セッション指標を生成（source/medium/campaign 指標）
- [ ] export_rollup.ts がロールアップ結果を CSV/JSON で出力
- [ ] export.ts が D1 の SELECT 結果をストリームで吐き出す
- [ ] fields.md に UTM/参照元/クリックの標準カラム定義（型/NULL許容）

受け入れ基準（DoD）
- [ ] 期間を指定した UTM 集計が得られる
- [ ] CSV/JSON のエクスポートがダウンロード可能

ロールバック
- jobs を無効化しても基盤は影響なし。
