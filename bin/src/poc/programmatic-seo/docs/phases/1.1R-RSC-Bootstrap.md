DELETE THIS FILE AFTER PHASE 1.1R IS COMPLETED

Phase 1.1R — RSC/SSR Bootstrap（任意の前倒し統合）

目的
- ゼロCF方針でも、RSC/SSR の配信・アセット・ルーティング・最低限 UI を先に固定。

スコープ
- interfaces/site に rwsdk-init を導入（この段階ではデプロイ任意）
- packages/pseo-templates の要件を UI に接続（差し込み位置の確認）

作業ツリー（作成）
- interfaces/site/ （rwsdk-init の内容を配置）
- interfaces/site/public/ （robots, icons, gsc プレースホルダ）
- interfaces/site/edge-functions/[[path]].ts （後続 1.2 で有効化する空枠）

実装チェックリスト
- [ ] rwsdk-init の dev/build が interfaces/site で通る
- [ ] SSR/Assets/Routing がプレビュー環境で動作
- [ ] packages/pseo-templates の spec（gallery/work/author）が UI で満たされる

受け入れ基準（DoD）
- [ ] 主要ページで JSON‑LD 差し込み位置が確定
- [ ] ナビ/内部リンク骨格が要件を満たす

ロールバック
- interfaces/site を無効化（ビルド対象から外す）すれば 1.0/1.1 の状態へ戻る。

備考
- 本フェーズは任意。1.2 でまとめて統合でもよい。
