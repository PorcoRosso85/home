{
  "name": "event-sourcing-kuzu-wasm",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test:old": "node --test *.test.cjs",
    "test": "deno test --allow-read --allow-net --no-check *.test.ts",
    "test:watch": "deno test --allow-read --allow-net --no-check --watch *.test.ts"
  },
  "dependencies": {
    "kuzu-wasm": "latest"
  }
}