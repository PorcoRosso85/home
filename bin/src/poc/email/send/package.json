{
  "name": "@poc/email-send",
  "version": "1.0.0",
  "type": "module",
  "main": "src/main.ts",
  "scripts": {
    "start": "bun run src/main.ts",
    "dry-run": "bun run src/main.ts --dry-run",
    "help": "bun run src/main.ts --help",
    "test": "bun test",
    "build": "bun build src/main.ts --outdir=dist --target=bun"
  },
  "dependencies": {
    "@aws-sdk/client-ses": "^3.632.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.5.4"
  }
}