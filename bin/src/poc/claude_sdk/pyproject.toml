[project]
name = "claude-sdk-poc"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "anthropic>=0.39.0",
    "requests>=2.31.0",
    "claude-code-sdk>=0.0.12",
    "anyio>=4.0.0",
    "pytest>=7.0.0",
]

[tool.uv]
managed = true