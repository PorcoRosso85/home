{
  "name": "claude-sdk-ts-poc",
  "version": "1.0.0",
  "description": "Claude Code SDK TypeScript POC with session continuation and streaming",
  "type": "module",
  "scripts": {
    "claude": "tsx src/claudeContinue.ts",
    "test": "tsx --test src/sessionManager.ts",
    "test:py": "cd . && python -m pytest session.py -v"
  },
  "dependencies": {
    "@anthropic-ai/claude-code": "*"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "tsx": "^4.0.0"
  }
}