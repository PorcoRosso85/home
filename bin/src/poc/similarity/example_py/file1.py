def calculate_total(items):
    total = 0
    for item in items:
        total += item
    return total

def compute_sum(numbers):
    sum_value = 0
    for num in numbers:
        sum_value += num
    return sum_value

def add_numbers(arr):
    result = 0
    for n in arr:
        result = result + n
    return result